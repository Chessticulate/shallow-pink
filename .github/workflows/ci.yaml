name: libchess-ci
on:
    push:
        branches: [ '**' ]

jobs:
    test:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v3
            with:
                node-version: 16.x
            - name: Install Dependencies
              run: npm ci
            - name: Run Tests
              run: npm test

